# AGENTS.md

## Instructions
- After each feature change, create a git commit with a sensible message and push it.
- If a push fails due to sandbox networking, retry `git push` with escalated permissions.
